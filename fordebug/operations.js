let addRes = add(100,200);
console.log(`Add = ${addRes}`); 

let subRes = sub(300,200);
console.log(`Sub = ${subRes}`);


function add (x,y){
    return x + y;
}

function sub(x,y){
    return x - y;
}